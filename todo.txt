TODO — d3i

[typed ids] DONE — `ref X` now generates a strongly-typed id
    - EntityId<TEntity> added to PolyPersist/interfaces/dotnet/PolyPersist/Core/EntityId.cs
      (readonly record struct over a string, implicit string operators, IParsable,
      one open-generic JsonConverterFactory). UNCONSTRAINED — intentional: entities get
      their base by MODELING it (e.g. `inherits Core.Base.BaseEntity`), not from a
      hardcoded PolyPersist.IEntity, so a `where TEntity : IEntity` constraint would not
      fit the modeled-base approach.
    - .NET emitter: `ref <Aggregate>` -> `EntityId<<root entity>>`, resolved to the
      aggregate's ROOT entity (the class that actually carries the @id), fully qualified,
      + using PolyPersist.Net.Core.
    - Proto/TS emitters: a ref is the referenced aggregate's id -> `string` on the wire.
  Optional future (PolyPersist adaptation):
    - If the platform ever standardizes all persistable entities on one base interface,
      add the matching `where TEntity : ...` constraint to EntityId<TEntity>.
    - Consider PolyPersist store/collection apis accepting EntityId<T> (typed) alongside
      `string id` (Find/Delete/Get), leaning on the implicit string conversion.

[done] `validate` evaluation/codegen: parse tree -> validate_node AST (ElementBuilder);
       TYPE-AWARE lint (identifiers resolve to value/sibling; operations must fit the field
       type: ordering only on number/date, len only on string/list/map, matches only on
       string, == on same category; top-level boolean); .NET codegen -> PolyPersist.IValidable
       with readable guards (negation folded into operators, bare field names, minimal parens,
       len -> .Length for string / .Count for list/map; @optional -> null-guarded).
       Base-class chaining DONE (.NET): a value object/entity inheriting a validating base
       emits `override` + `base.Validate(errors)` first (base is `virtual`); multi-level
       chains. NOTE: DTOs cannot inherit in the grammar (the `dto` rule has no `inherits`),
       so there is no TS-side chaining to do — DTO validators are flat.
       Negative numeric literals in validate: DONE (added MINUS token + `MINUS? INTEGER/NUMBER`
       in validate_term; e.g. `value >= -273`, `value IN -180..180`).
       The `validate` feature is now complete.

[done] DTO client-side validation: a dto's OWN `validate` rules (not derived from the
    domain) generate a TypeScript validator `validate<Dto>(dto): ValidationError[]` in the
    TS emitter, same readable/negated shape as the .NET side (dto. access, len -> .length /
    Object.keys().length, matches -> new RegExp().test). Same type-aware lint applies.
    The server (domain IValidable) is still the authoritative validator. Needed a grammar
    fix: dto_member now allows `(VALIDATE validate_expr)?` (regenerated the parser).

[deferred language codegen] see memory microniq-current-work.md:
    - workflow Temporal runtime implementation
    - LSP: language_server.py calls main_module.lsp_initialize / lsp_did_open_text which
      are not defined in __main__.py -> broken; fix + smoke test.

[done] interface surface lint: an interface may only expose dto/enum/primitive types
       (plus `ref` -> string, and list/map of those); domain types (value_object,
       entity, view, composite, aggregate) may not leak onto the wire surface.
