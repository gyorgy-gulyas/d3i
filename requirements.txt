# Local development setup.
#
# Runtime dependencies are declared in pyproject.toml ([project.dependencies]).
# This file installs the package in editable mode together with the optional
# dev/test and language-server extras, so `pip install -r requirements.txt`
# gives a full development environment.
-e .[dev,lsp]
